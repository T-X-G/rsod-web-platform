# 协作指南

1. 克隆仓库：`git clone git@github.com:T-X-G/rsod-web-platform.git`
2. 创建分支：`git checkout -b feature/你的功能名`
3. 开发并提交：`git commit -m "[模块] 说明"`
4. 推送分支：`git push origin feature/你的功能名`
5. 在 GitHub 上发起 Pull Request 到 `main` 分支
6. 等待至少2位成员审查通过后合并
